{
  "name": "@proscar87/oura-mcp",
  "version": "0.3.2",
  "description": "The Oura v2 API as an MCP server. Paginates, fixes the date range, warns when data is missing.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "oura-mcp": "dist/main.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "check": "node dist/main.js --check"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
